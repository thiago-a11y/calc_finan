"""
Regras compartilhadas por todos os agentes do Synerium Factory.

Thin re-export de core.prompts.rules (Fase 2.1 — v0.59.0).
Mantido para backward compatibility — todos os callers continuam funcionando.
"""

from core.prompts.rules import REGRAS_ANTI_ALUCINACAO  # noqa: F401
